{
  "name": "realms-of-might-and-magic",
  "version": "1.0.0",
  "description": "",
  "main": "socket-server.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:server": "npx nodemon socket-server.ts",
    "start:client": "cd romm && npm run dev"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "socket.io": "^4.5.4"
  },
  "devDependencies": {
    "nodemon": "^2.0.20",
    "npm-run-all": "^4.1.5"
  }
}
